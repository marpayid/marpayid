
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Informasi profil pengguna MarPay.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "ID unik pengguna dari Firebase Auth."
        },
        "name": {
          "type": "string",
          "description": "Nama lengkap pengguna."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Alamat email pengguna."
        },
        "phone": {
          "type": "string",
          "description": "Nomor telepon/WhatsApp pengguna."
        },
        "photoURL": {
          "type": "string",
          "description": "URL foto profil pengguna."
        },
        "accountStatus": {
          "type": "string",
          "enum": ["active", "suspended"],
          "description": "Status akun pengguna."
        },
        "role": {
          "type": "string",
          "enum": ["customer", "admin"],
          "description": "Peran pengguna dalam aplikasi."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["uid", "name", "email", "phone"]
    },
    "Address": {
      "title": "Address",
      "description": "Data alamat pengiriman pengguna.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "province": { "type": "string" },
        "city": { "type": "string" },
        "district": { "type": "string" },
        "fullAddress": { "type": "string" },
        "notes": { "type": "string" },
        "isPrimary": { "type": "boolean" }
      }
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Koleksi data profil pengguna."
    },
    "/users/{userId}/addresses/{addressId}": {
      "schema": {
        "$ref": "#/entities/Address"
      },
      "description": "Sub-koleksi alamat pengiriman pengguna."
    }
  }
}
